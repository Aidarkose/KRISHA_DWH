version: 2

models:
  - name: metricflow_time_spine
    description: "Time spine для MetricFlow (один день на строку, 2020–2030)."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
        tests:
          - unique
          - not_null
