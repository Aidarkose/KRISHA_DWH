-- Generated by AutomateDV (formerly known as dbtvault)

    

WITH source_data AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at

    FROM "krisha_dwh"."stg"."listings"
),

derived_columns AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    source_fetched_at AS LOAD_DATETIME,
    'stg.listings' AS RECORD_SOURCE

    FROM source_data
),

hashed_columns AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    LOAD_DATETIME,
    RECORD_SOURCE,

    DECODE(MD5(NULLIF(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(listing_id AS VARCHAR))), ''), '^^')
    ), '^^')), 'hex') AS HK_LISTING,

    DECODE(MD5(NULLIF(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(city_name AS VARCHAR))), ''), '^^')
    ), '^^')), 'hex') AS HK_CITY,

    DECODE(MD5(NULLIF(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(listing_id AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(city_name AS VARCHAR))), ''), '^^')
    ), '^^||^^')), 'hex') AS HK_LISTING_CITY,

    DECODE(MD5(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(area_kitchen_m2 AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(area_living_m2 AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(area_total_m2 AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(balcony AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(bathroom AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(build_year AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(building_type AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(category AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(ceiling_height_m AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(complex_name AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(deal_type AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(description_hash AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(district_name AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(floor AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(floors_total AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(furniture AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(parking AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(property_type AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(renovation AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(rooms AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(seller_type AS VARCHAR))), ''), '^^')
    )), 'hex') AS HD_LISTING_ATTRS

    FROM derived_columns
),

columns_to_select AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    LOAD_DATETIME,
    RECORD_SOURCE,
    HK_LISTING,
    HK_CITY,
    HK_LISTING_CITY,
    HD_LISTING_ATTRS

    FROM hashed_columns
)

SELECT * FROM columns_to_select