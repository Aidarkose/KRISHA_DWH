-- Generated by AutomateDV (formerly known as dbtvault)

    

WITH source_data AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at

    FROM "krisha_dwh"."stg"."listings"
),

derived_columns AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    source_fetched_at AS LOAD_DATETIME,
    'stg.listings' AS RECORD_SOURCE

    FROM source_data
),

hashed_columns AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    LOAD_DATETIME,
    RECORD_SOURCE,

    DECODE(MD5(NULLIF(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(listing_id AS VARCHAR))), ''), '^^')
    ), '^^')), 'hex') AS HK_LISTING,

    DECODE(MD5(CONCAT_WS('||',
        COALESCE(NULLIF(UPPER(TRIM(CAST(price_kzt AS VARCHAR))), ''), '^^'),
        COALESCE(NULLIF(UPPER(TRIM(CAST(status AS VARCHAR))), ''), '^^')
    )), 'hex') AS HD_LISTING_PRICE

    FROM derived_columns
),

columns_to_select AS (

    SELECT

    listing_id,
    category,
    deal_type,
    property_type,
    price_kzt,
    currency,
    rooms,
    area_total_m2,
    area_living_m2,
    area_kitchen_m2,
    floor,
    floors_total,
    building_type,
    build_year,
    ceiling_height_m,
    bathroom,
    furniture,
    renovation,
    balcony,
    parking,
    city_id,
    city_name,
    district_name,
    address_text,
    complex_name,
    lat,
    lon,
    description,
    description_hash,
    photos_count,
    photos_urls,
    seller_type,
    seller_name,
    posted_at,
    status,
    url,
    source_fetched_at,
    updated_at,
    LOAD_DATETIME,
    RECORD_SOURCE,
    HK_LISTING,
    HD_LISTING_PRICE

    FROM hashed_columns
)

SELECT * FROM columns_to_select